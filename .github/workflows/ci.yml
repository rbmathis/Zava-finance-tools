name: CI
on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Build
        run: echo "Building Zava-finance-tools..."
      - name: Test
        run: echo "Testing Zava-finance-tools... All tests passed."
